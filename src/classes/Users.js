import { LinkedList } from "./LinkedList";
//import users from "../data/userData.json";
//import users02 from "../data/userData02.json";
//import users03 from "../data/userData03.json";
//import users04 from "../data/userData04.json";

/* export const testUsers10K = () => {
//Logic for users using a Linked List
let userLinkedList = new LinkedList();
//Initialize time
console.time('fill')
//Fill with 10000 data test 
users.map(user => {
    userLinkedList.pushBack(user)
})
//End process
console.timeEnd('fill')

//Traverse all the list  
//Init time 
console.time('traverse')
userLinkedList.traverse();
//End process
console.timeEnd('traverse')    

//Search element from data
console.time('search')
userLinkedList.search("Mrs. Reva Daugherty");
console.timeEnd('search') 

//Update a node key 
console.time('update')
userLinkedList.updateNode("Mrs. Reva Daugherty", "Nicolas");
console.timeEnd('update')

//Remove element at index
console.time('delete')
userLinkedList.removeNode(9999);
console.timeEnd('delete')
} */
 

/* export const testUsers100K = () => {
let userLinkedList = new LinkedList();
//Initialize time
console.time('fill')
//Fill with 100K data test 
users02.map(user => {
    userLinkedList.pushBack(user) // O(1) Operation 
})
//End process
console.timeEnd('fill')

//Traverse all the list 
//Init time 
console.time('traverse')
userLinkedList.traverse(); // O(n) Operation
//End process
console.timeEnd('traverse')   

//Search element from data
console.time('search')
userLinkedList.search("Jovan Keebler");
console.timeEnd('search') 

//Update a node key 
console.time('update')
userLinkedList.updateNode("Jovan Keebler", "Nicolas");
console.timeEnd('update')

//Remove element at index
console.time('delete')
userLinkedList.removeNode(99999);
console.timeEnd('delete')
} */
/* export const testUsers500K = () => {
    let userLinkedList = new LinkedList();

    //Initialize time
    console.time('fill')
    //Fill with 500K data test 
    users03.map(user => {
        userLinkedList.pushBack(user)
    })
    //End process
    console.timeEnd('fill')    

    //Traverse all the list 
    //Init time 
    console.time('traverse')
    userLinkedList.traverse()
    //End process
    console.timeEnd('traverse')

    //Search element from data
     console.time('search')
     userLinkedList.search("Mrs. Mariela Gulgowski");
     console.timeEnd('search') 

    //Update a node key 
    console.time('update')
    userLinkedList.updateNode("Mrs. Mariela Gulgowski", "Nicolas");
    console.timeEnd('update')

    //Remove element at index
    console.time('delete')
    userLinkedList.removeNode(499999);
    console.timeEnd('delete')
} */

/* export const testUsers1M = () => {
    let userLinkedList = new LinkedList();
    //Initialize time
    console.time('fill')
    //Fill with 1M data test 
    users04.map(user => {
        userLinkedList.pushBack(user)
    })
    //End process
    console.timeEnd('fill')  

    //Traverse all the list 
    //Init time 
    console.time('traverse')
    userLinkedList.traverse();
    //End process
    console.timeEnd('traverse')  

    //Search element from data
     console.time('search')
     userLinkedList.search("Kallie Johnson");
     console.timeEnd('search') 

    //Update a node key (key value, new key value)
    console.time('update')
    userLinkedList.updateNode("Kallie Johnson", "Nicolas");
    console.timeEnd('update')

    //Remove element at index
    console.time('delete')
    userLinkedList.removeNode(999999);
    console.timeEnd('delete')
} */


